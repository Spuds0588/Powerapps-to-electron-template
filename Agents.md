# Agent Directives: Power App Electron Extension

## Project Overview
You are acting as an expert Electron & Node.js developer assisting in the creation of a desktop
wrapper for Microsoft Power Apps. The app uses a dual-pane layout (`WebContentsView`) to display
a Power App in a persistent sidebar and an external browser in the main window. The main process
scrapes the main window and passes context back into the Power App as URL query parameters.

This repository is the **sister project** of `Spuds0588/Powerapp-to-Extension-Template` (the Chrome
extension version). Where possible, replicate that project's conventions: numbered README sections,
`[PowerApps ...]` console log prefixes, a `config` file that is heavily commented with safe defaults,
the vanilla single-file configuration wizard, and the `Research_Tools` bookmarklets.

## Architectural Constraints & Rules
1.  **YAGNI (You Aren't Gonna Need It):** Keep solutions strictly to one-liners or minimal
    implementations where possible. Do not over-engineer.
2.  **No IPC / Preload Scripts:** Do NOT use `preload.js` or `ipcMain`/`ipcRenderer` for context
    scraping. We exclusively use `webContents.executeJavaScript` directly from `main.js` to extract
    data from the main window.
3.  **Modern Electron APIs:** Always use `WebContentsView` (attached to `BaseWindow.contentView`)
    instead of `BrowserView` (deprecated) or `<webview>` / `<iframe>` tags.
4.  **State Preservation:** The Power App is a heavy Canvas application. Never call a reload or
    `loadURL` on the sidebar unless the underlying injected URL parameters have explicitly changed.
5.  **Popup Handling:** Microsoft SSO requires popups. Ensure `setWindowOpenHandler` always returns
    `{ action: 'allow' }` for auth flows.
6.  **Configuration:** Settings are read from a local `config.json` generated by the web-based
    wizard. Always deep-merge the file over hard-coded defaults so a missing or partial
    `config.json` never crashes the app.
7.  **The Wizard:** `wizard.html` must remain a vanilla, zero-dependency, single-file HTML
    application relying on the native File System Access API (`window.showDirectoryPicker`). It must
    be deployable to GitHub Pages without a backend, with a `Blob` download fallback. No CDN pulls.
8.  **Wizard Parity:** The wizard mirrors the sister project's step-by-step experience (hero screen,
    Previous/Next navigation, conditional steps, a simulated preview, distro tabs) and its light
    palette. The simulator must reuse the app's logic, not approximate it: `toConfig`,
    `buildScrapeResult` and `buildSidebarUrl` are exposed on `window.PowerAppWizard` and
    `test/static.test.js` asserts they produce byte-identical URLs to `main.js`.
9.  **Style:** Follow the established color system (Savoy Blue `#5964A6`, Penn Blue `#0C1B49`,
    Platinum `#DFE1E8`, Resolution Blue `#182575`, International Klein Blue `#232F9F`).

## File Map
| File | Purpose |
| --- | --- |
| `main.js` | Electron main process: window, views, scraping, triggers. |
| `config.json` | User settings generated by the wizard (git-ignored). |
| `config.example.json` | Committed reference/default configuration. |
| `wizard.html` | Single-file visual configuration wizard (GitHub Pages). |
| `forge.config.js` | Electron Forge packaging/maker configuration. |
| `Research_Tools/` | Bookmarklets to discover DOM IDs and local storage keys. |
| `test/` | `npm test` suite. `harness.js` loads `main.js` in a `node:vm` sandbox with a stubbed Electron, and `wizard.html` with a stubbed DOM. |## Verification
*   `npm test` must pass before a change is considered done. Add a case to `test/main.test.js` when
    touching `main.js`, to `test/static.test.js` when touching shipped JS/HTML/JSON, and to
    `test/wizard.test.js` when changing the wizard's steps or flow.
*   `test/harness.js` is the only place Electron is faked. Keep `main.js` free of top-level side
effects beyond `app.whenReady()` so the harness can keep loading it directly.

## Common Testing Scenarios
*   **"Blank screen when logging in"** — check the `setWindowOpenHandler` popup logic on both views.
*   **"Wizard cannot save file"** — verify the user is on a Secure Context (HTTPS / GitHub Pages)
    with a Chromium-based browser that supports the File System Access API. Offer the download
    fallback.
*   **"Power App keeps refreshing and I lose my text"** — verify the conditional check that prevents
    identical-URL reloads is functioning in the `updatePowerApp()` loop.
*   **"Nothing is scraped"** — verify `includeTabBody` is enabled, `targetIds` are populated, and
    the target page is not a cross-origin `iframe` (standard CORS policies apply).
